<%= form_with(model: script, local: true) do |form| %>
  <% if script.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(script.errors.count, "error") %> prohibited this script from being saved:</h2>

      <ul>
      <% script.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :script_name %>
  </div>

  <div class="field">
    <%= form.label :provider %>
    <%= form.text_field :provider, id: :script_provider %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
