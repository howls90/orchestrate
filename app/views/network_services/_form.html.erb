<%= form_with(model: network_service, local: true) do |form| %>
  <% if network_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_service.errors.count, "error") %> prohibited this network_service from being saved:</h2>

      <ul>
      <% network_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :network_service_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
